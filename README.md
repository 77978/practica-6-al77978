El usuario ingresa los valores de cohesión (c), peso unitario (γ), ancho de zapata (B), profundidad de desplante (Df) y ángulo de fricción interna (φ). Al presionar el botón “Calcular”, el programa ejecuta una serie de funciones que procesan los datos de la siguiente manera:

Cálculo de los factores de capacidad portante (Nc, Nq y Nγ):
Se determinan a partir del ángulo de fricción interna del suelo mediante fórmulas empíricas derivadas del método de Terzaghi.

Aplicación de la fórmula de Terzaghi:
Con los factores obtenidos, se calcula la capacidad portante última (qₙₜ) y la capacidad portante admisible (qₐdₘ), considerando un factor de seguridad (FS = 3).
Presentación de resultados:
Los valores calculados se muestran en pantalla de forma clara y legible dentro de la misma ventana. Si el usuario introduce datos no válidos, el sistema muestra un mensaje de error.

La interfaz está diseñada con un formato sencillo y colores suaves, facilitando su uso y lectura. 
